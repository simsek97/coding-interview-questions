# The First Character that does not repeat in a Stream

## Question

Given an arbitrary length input stream of characters, return the first character that does not repeat anywhere in the stream.

## Solution

- The first solution finds the first non-repeating character in a iterative way
- The second solution uses a queue to find the first non-repeating character
